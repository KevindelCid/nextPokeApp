import React from 'react';
import { Layout } from '../../components/layouts';

const Favorites = () => {
    return (
        <Layout title='favorites'>
            Hola estos son los favoritos
        </Layout>
    );
};

export default Favorites;